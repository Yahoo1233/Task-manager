export interface TaskItem {
    id: string,
    task: string,
    time: string,
    completed: boolean;
    // Добавить сроность задач (чек-бокс)
    // Уровень важность (3 уровня)
    // добавить теги (#Работа, #Учеба)
    // описание задачи 
    // засунуть форму для создания задачи в модальное окно
}